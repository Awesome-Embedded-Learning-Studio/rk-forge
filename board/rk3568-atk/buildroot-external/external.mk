# rk-forge BR2_EXTERNAL makefile (rk3568-atk).
#
# Currently carries only the board defconfig (configs/rk3568_atk_defconfig) — no
# custom packages yet. As the buildroot line grows (Phase 2: librknnrt for the NPU,
# mesa/Panfrost userspace, gstreamer/ffmpeg v4l2-stateless), add them here. See
# buildroot docs/manual/customize-outsideroot.txt for the BR2_EXTERNAL extension points.
