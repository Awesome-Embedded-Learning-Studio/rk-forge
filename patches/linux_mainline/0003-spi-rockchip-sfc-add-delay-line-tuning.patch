From 9adbadcabf1f86cef832d8b7d35ec49b469946e6 Mon Sep 17 00:00:00 2001
From: CharlieChen114514 <725610365@qq.com>
Date: Mon, 15 Jun 2026 23:32:23 +0800
Subject: [PATCH 1/3] spi: rockchip-sfc: add sampling delay-line tuning for
 SFC_VER_4+

The mainline rockchip_sfc driver defines the SFC_DLL_CTRL0 (0x3C) register
and its SCLK_SMP_DLL bit but never programs them, so the sampling delay line
is left untrained. Above ~50 MHz this is marginal: on the RK3506B SoC's SFC
(VER_5) reading a Winbond W25N04KV SPI-NAND at 80 MHz produces sporadic bit
errors that exceed the flash's on-die ECC and surface as UBI -EBADMSG read
errors (PEB corruption, occasionally degrading to a 0xee SFC latch-up). The
only workaround until now was capping spi-max-frequency at 50 MHz in DT.

Port the vendor BSP's rockchip_sfc_delay_lines_tuning() (from the Rockchip
kernel-6.1 spi-rockchip-sfc.c) into the mainline driver: when the rate rises
above SFC_DLL_THRESHOLD_RATE (50 MHz) on a VER_4+ controller, scan the delay
cells, read the JEDEC ID (0x9F) at each, and pick the middle of the widest
contiguous valid window (matching the vendor's left-biased pick when the
window starts at cell 0). If no SFC_DLL_TRANING_VALID_WINDOW-wide window is
found, fall back to 50 MHz where sampling is stable untrained.

The scan uses a small rockchip_sfc_exec_op_nofreq() helper that runs a single
PIO op without re-entering the frequency/tuning logic (so training can issue
its own JEDEC reads without recursing). Trained cells are cached per CS and
re-applied on resume (DLL state is lost on power-down). The optional vendor
DT property "rockchip,max-dll" caps the cell scan; absent it, the max is taken
from the SFC version (0xFF for VER_4, 0x1FF for VER_5).

This keeps the mainline "rockchip,sfc" binding; the vendor BSP instead uses a
private "fspi" driver. Only the tuning logic is ported.

Signed-off-by: CharlieChen114514 <725610365@qq.com>
---
 drivers/spi/spi-rockchip-sfc.c | 162 +++++++++++++++++++++++++++++++++
 1 file changed, 162 insertions(+)

diff --git a/drivers/spi/spi-rockchip-sfc.c b/drivers/spi/spi-rockchip-sfc.c
index 174995042..76076ad5a 100644
--- a/drivers/spi/spi-rockchip-sfc.c
+++ b/drivers/spi/spi-rockchip-sfc.c
@@ -169,6 +169,16 @@
  */
 #define SFC_MAX_SPEED		(150 * 1000 * 1000)
 
+/* Sampling delay-line tuning (SFC_VER_4+). Below SFC_DLL_THRESHOLD_RATE the
+ * sampling is stable without tuning; above it the SCLK_SMP_DLL delay line must
+ * be trained by scanning delay cells and picking the middle of the widest
+ * contiguous valid window (verified by reading back the JEDEC ID 0x9F).
+ * Ported from the Rockchip vendor kernel; see the commit log.
+ */
+#define SFC_DLL_THRESHOLD_RATE		(50 * 1000 * 1000)
+#define SFC_DLL_TRANING_STEP		10	/* delay-cell step while scanning */
+#define SFC_DLL_TRANING_VALID_WINDOW	80	/* min contiguous good cells */
+
 #define ROCKCHIP_AUTOSUSPEND_DELAY	2000
 
 struct rockchip_sfc {
@@ -183,6 +193,8 @@ struct rockchip_sfc {
 	struct completion cp;
 	bool use_dma;
 	u32 max_iosize;
+	u32 max_dll_cells;
+	u32 dll_cells[SFC_MAX_CHIPSELECT_NUM];
 	u16 version;
 	struct spi_controller *host;
 };
@@ -218,6 +230,33 @@ static u32 rockchip_sfc_get_max_iosize(struct rockchip_sfc *sfc)
 	return SFC_MAX_IOSIZE_VER3;
 }
 
+static u32 rockchip_sfc_get_max_dll_cells(struct rockchip_sfc *sfc)
+{
+	if (sfc->max_dll_cells)
+		return sfc->max_dll_cells;
+
+	if (sfc->version > SFC_VER_4)
+		return SFC_DLL_CTRL0_DLL_MAX_VER5;
+	else if (sfc->version == SFC_VER_4)
+		return SFC_DLL_CTRL0_DLL_MAX_VER4;
+	else
+		return 0;
+}
+
+static void rockchip_sfc_set_delay_lines(struct rockchip_sfc *sfc, u16 cells, u8 cs)
+{
+	u16 cell_max = (u16)rockchip_sfc_get_max_dll_cells(sfc);
+	u32 val = 0;
+
+	if (cells > cell_max)
+		cells = cell_max;
+
+	if (cells)
+		val = SFC_DLL_CTRL0_SCLK_SMP_DLL | cells;
+
+	writel(val, sfc->regbase + cs * SFC_CS1_REG_OFFSET + SFC_DLL_CTRL0);
+}
+
 static int rockchip_sfc_clk_set_rate(struct rockchip_sfc *sfc, unsigned long  speed)
 {
 	if (sfc->version >= SFC_VER_8)
@@ -521,6 +560,114 @@ static int rockchip_sfc_xfer_done(struct rockchip_sfc *sfc, u32 timeout_us)
 	return ret;
 }
 
+/* Run a single op with the controller already clocked, bypassing the per-op
+ * frequency/tuning logic (so delay-line training can issue its own JEDEC-ID
+ * reads without recursing back into the tuner). PIO only — matches the
+ * rockchip,sfc-no-dma bring-up and avoids touching the DMA buffer.
+ */
+static int rockchip_sfc_exec_op_nofreq(struct rockchip_sfc *sfc,
+				       struct spi_mem *mem,
+				       const struct spi_mem_op *op)
+{
+	u32 len = min_t(u32, op->data.nbytes, sfc->max_iosize);
+	int ret;
+
+	rockchip_sfc_adjust_op_work((struct spi_mem_op *)op);
+	rockchip_sfc_xfer_setup(sfc, mem, op, len);
+	ret = rockchip_sfc_xfer_data_poll(sfc, op, len);
+	if (ret != len) {
+		dev_err(sfc->dev, "xfer data failed ret %d\n", ret);
+		return -EIO;
+	}
+
+	return rockchip_sfc_xfer_done(sfc, 100000);
+}
+
+/* Train the SCLK_SMP_DLL delay line at the current clock rate by scanning
+ * delay cells and reading back the JEDEC ID (0x9F); the good window is the
+ * run of cells that returns a stable, matching ID. Pick its middle (the
+ * vendor biases toward the low end when the window starts at cell 0). If no
+ * valid window of SFC_DLL_TRANING_VALID_WINDOW cells is found, fall back to
+ * SFC_DLL_THRESHOLD_RATE where sampling is stable untrained.
+ */
+static void rockchip_sfc_delay_lines_tuning(struct rockchip_sfc *sfc, struct spi_mem *mem)
+{
+	struct spi_mem_op op = SPI_MEM_OP(SPI_MEM_OP_CMD(0x9F, 1),
+					 SPI_MEM_OP_NO_ADDR,
+					 SPI_MEM_OP_NO_DUMMY,
+					 SPI_MEM_OP_DATA_IN(3, NULL, 1));
+	u8 id[3], id_temp[3];
+	u16 cell_max = (u16)rockchip_sfc_get_max_dll_cells(sfc);
+	u16 right, left = 0;
+	u16 step = SFC_DLL_TRANING_STEP;
+	bool dll_valid = false;
+	u8 cs = spi_get_chipselect(mem->spi, 0);
+
+	rockchip_sfc_clk_set_rate(sfc, SFC_DLL_THRESHOLD_RATE);
+	op.data.buf.in = &id;
+	rockchip_sfc_exec_op_nofreq(sfc, mem, &op);
+	if ((0xFF == id[0] && 0xFF == id[1]) ||
+	    (0x00 == id[0] && 0x00 == id[1])) {
+		dev_dbg(sfc->dev, "no dev, dll by pass\n");
+		rockchip_sfc_clk_set_rate(sfc, sfc->speed[cs]);
+		sfc->speed[cs] = SFC_DLL_THRESHOLD_RATE;
+
+		return;
+	}
+
+	rockchip_sfc_clk_set_rate(sfc, sfc->speed[cs]);
+	op.data.buf.in = &id_temp;
+	for (right = 0; right <= cell_max; right += step) {
+		int ret;
+
+		rockchip_sfc_set_delay_lines(sfc, right, cs);
+		rockchip_sfc_exec_op_nofreq(sfc, mem, &op);
+		dev_dbg(sfc->dev, "dll read flash id:%x %x %x\n",
+			id_temp[0], id_temp[1], id_temp[2]);
+
+		ret = memcmp(&id, &id_temp, 3);
+		if (dll_valid && ret) {
+			right -= step;
+
+			break;
+		}
+		if (!dll_valid && !ret)
+			left = right;
+
+		if (!ret)
+			dll_valid = true;
+
+		/* Make sure cell_max itself is sampled. */
+		if (right == cell_max)
+			break;
+		if (right + step > cell_max)
+			right = cell_max - step;
+	}
+
+	if (dll_valid && (right - left) >= SFC_DLL_TRANING_VALID_WINDOW) {
+		if (left == 0 && right < cell_max)
+			sfc->dll_cells[cs] = left + (right - left) * 2 / 5;
+		else
+			sfc->dll_cells[cs] = left + (right - left) / 2;
+	} else {
+		sfc->dll_cells[cs] = 0;
+	}
+
+	if (sfc->dll_cells[cs]) {
+		dev_dbg(sfc->dev, "%d %d %d dll training success in %dMHz max_cells=%u sfc_ver=%d\n",
+			left, right, sfc->dll_cells[cs], sfc->speed[cs],
+			rockchip_sfc_get_max_dll_cells(sfc), rockchip_sfc_get_version(sfc));
+		rockchip_sfc_set_delay_lines(sfc, (u16)sfc->dll_cells[cs], cs);
+	} else {
+		dev_err(sfc->dev, "%d %d dll training failed in %dMHz, reduce the frequency\n",
+			left, right, sfc->speed[cs]);
+		rockchip_sfc_set_delay_lines(sfc, 0, cs);
+		rockchip_sfc_clk_set_rate(sfc, SFC_DLL_THRESHOLD_RATE);
+		mem->spi->max_speed_hz = SFC_DLL_THRESHOLD_RATE;
+		sfc->speed[cs] = SFC_DLL_THRESHOLD_RATE;
+	}
+}
+
 static int rockchip_sfc_exec_mem_op(struct spi_mem *mem, const struct spi_mem_op *op)
 {
 	struct rockchip_sfc *sfc = spi_controller_get_devdata(mem->spi->controller);
@@ -540,6 +687,12 @@ static int rockchip_sfc_exec_mem_op(struct spi_mem *mem, const struct spi_mem_op
 		if (ret)
 			goto out;
 		sfc->speed[cs] = op->max_freq;
+		if (rockchip_sfc_get_version(sfc) >= SFC_VER_4) {
+			if (rockchip_sfc_clk_get_rate(sfc) > SFC_DLL_THRESHOLD_RATE)
+				rockchip_sfc_delay_lines_tuning(sfc, mem);
+			else
+				rockchip_sfc_set_delay_lines(sfc, 0, cs);
+		}
 		dev_dbg(sfc->dev, "set_freq=%dHz real_freq=%ldHz\n",
 			sfc->speed[cs], rockchip_sfc_clk_get_rate(sfc));
 	}
@@ -657,6 +810,10 @@ static int rockchip_sfc_probe(struct platform_device *pdev)
 
 	sfc->use_dma = !of_property_read_bool(sfc->dev->of_node, "rockchip,sfc-no-dma");
 
+	device_property_read_u32(&pdev->dev, "rockchip,max-dll", &sfc->max_dll_cells);
+	if (sfc->max_dll_cells > SFC_DLL_CTRL0_DLL_MAX_VER5)
+		sfc->max_dll_cells = SFC_DLL_CTRL0_DLL_MAX_VER5;
+
 	ret = clk_prepare_enable(sfc->hclk);
 	if (ret) {
 		dev_err(&pdev->dev, "Failed to enable ahb clk\n");
@@ -790,6 +947,7 @@ static int rockchip_sfc_suspend(struct device *dev)
 static int rockchip_sfc_resume(struct device *dev)
 {
 	struct rockchip_sfc *sfc = dev_get_drvdata(dev);
+	u32 i;
 	int ret;
 
 	ret = pm_runtime_force_resume(dev);
@@ -805,6 +963,10 @@ static int rockchip_sfc_resume(struct device *dev)
 	}
 
 	rockchip_sfc_init(sfc);
+	/* DLL cell settings are lost on power-down; restore the trained ones. */
+	for (i = 0; i < SFC_MAX_CHIPSELECT_NUM; i++)
+		if (sfc->dll_cells[i])
+			rockchip_sfc_set_delay_lines(sfc, (u16)sfc->dll_cells[i], i);
 
 	pm_runtime_put_autosuspend(dev);
 
-- 
2.43.0

