#!/usr/bin/env bash
# flash-sd.sh — STUB (Week 3-4). Planned: lay out Rockchip boot on an SD card:
#   GPT + idbloader.img (offset 0x40) + u-boot.itb + kernel/DT/rootfs partitions.
# WSL2 note: SD device is visible (e.g. /dev/sdX via WSL2); USB/rkdeveloptool needs
# usbipd-win. doctor.sh detects WSL2.
set -euo pipefail
echo "flash-sd.sh: STUB (planned Week 3-4). See document/tutorial/boot/02_uboot_rkbin.md." >&2
exit 1
