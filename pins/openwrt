# pins/openwrt — OpenWrt source tree (rk3506 port).
# This repo already carries rk-forge's kernel patches 0001-0016 as quilt
# patches-7.1/series (verified byte-identical to patches/linux/), so OpenWrt's
# quilt applies them at build time — do NOT git-am the kernel patches here.
# A SMALL rk-forge overlay (Device/aes + config tweaks) lives in patches/openwrt/
# and is applied via apply-series.sh --component openwrt (git am, like linux/uboot).
#
# Format: <git-url> <ref>  (# = comment)
# TODO: fork to Awesome-Embedded-Learning-Studio for stability (same model as
# pins/rtl8733bu); for now pin upstream czz8888 directly so the integration is
# unblocked. The fork is a verbatim mirror — no in-fork edits.
https://github.com/czz8888/rk-3506-openwrt-7.1 31d15c0
