# rk-forge BR2_EXTERNAL makefile.
#
# Currently carries only the board defconfig (configs/rk3506_aes_defconfig) — no
# custom packages yet. As the buildroot line grows (per-board packages, rootfs
# overlays for mtdrawdump/mtdbb, post-build hooks), add them here. See buildroot
# docs/manual/customize-outsideroot.txt for the BR2_EXTERNAL extension points.
