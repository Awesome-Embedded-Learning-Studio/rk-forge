# document/notes — dated bringup log

This tier holds **honest, dated bringup notes** — what worked, what didn't, the exact
UART output, the brick scares. It's the project's honesty substrate (on-brand:
rk-forge's identity is "report the gap truthfully").

Convention: one file per session/milestone, dated (`YYYY-MM-DD-<slug>.md`). Don't
sanitize failures — they're the most useful part.

## First entries (to write)

- `2026-06-13-restructure.md` — repo restructure + premise verification.
- (after SDK pull) `YYYY-MM-DD-vendor-sdk-survey.md` — what the 正点原子/ALIENTEK (or
  other) SDK actually contains, whether it targets RK3506, its kernel version.
